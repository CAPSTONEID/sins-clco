# 썸네일 필수 스타일 선택

영상 제작에서 요청한 롱폼·숏폼 썸네일도 반드시 `/sins-contents-thumbstyle-prompt`를 읽고 제작합니다. 해당 스킬과 `sins-video-consource` 패키지에 같은 스타일 확인 절차를 반영했습니다.

## 필수 절차

- 새 썸네일 작업은 롱폼·숏폼·일반 가로형 모두 **스타일 질문 → 사용자 답변 → 제작** 순서를 따른다. 영상 제작 스킬에서 넘어와도 이 단계를 생략하지 않는다.
- 해당 사용처에 맞는 스타일 후보 2~3개를 추천 이유와 함께 제시하고, 추천안을 먼저 둔다. 롱폼은 내용에 맞는 실사·3D·두들·페이퍼 콜라주 등을 검토하고, 숏폼은 `references/legacy-thumbnail-formats.md`의 스타일을 따른다. 혼합 패키지는 적용 대상을 밝혀 하나로 묻거나 롱폼·숏폼을 나눠 묻는다.
- 질문 예: “이번 썸네일은 어떤 스타일로 만들까요? 선택한 스타일을 롱폼 3장과 숏폼 2장에 적용하겠습니다.” 장수와 대상은 실제 요청으로 바꾼다.
- **사용자 답변 전에는 스타일을 확정하거나 배경 생성·합성·렌더를 시작하지 않는다.** 내용 분석과 후보 제안은 진행할 수 있다. 질문 도구가 없으면 채팅으로 묻고 답을 기다린다. 무응답·기본 선택·시간 경과를 승인으로 해석하지 않는다.
- 영상 스타일·팔레트 선택, 모델·품질·비용 승인, “썸네일도 제작”, “추천 설정으로 진행”을 썸네일 스타일 선택으로 간주하지 않는다. 자동 추천 요청이면 추천 스타일을 보여 주고 명시적 답변을 받는다.
- 사용자가 처음부터 특정 스타일을 지정했으면 그 스타일을 추천안으로 담아 적용 범위를 확인한다. 단, **이번 썸네일 작업의 스타일 질문에 이미 답했다면** 같은 질문을 반복하지 않고 그 답을 유지한다. 이후 문구·오탈자 수정만 할 때는 확정한 스타일을 이어받는다. 새 스타일로 바꾸는 경우 다시 묻는다.
- 기존 기획표 또는 프롬프트 TXT에 `thumbnail_style_question`, `thumbnail_style_answer`, `thumbnail_style_scope`를 기록한다. 사용자 답변이 없으면 `thumbnail_style_status: awaiting_user`이며 제작 완료로 처리하지 않는다. 답변과 범위가 있으면 `confirmed`로 기록한다.


## 타이틀은 효과 없이 글자만 합성 (필수)

- 롱폼·숏폼·일반 가로형 모두 타이틀 뒤에 별도 효과를 넣지 않는다. 제목용 박스·띠·반투명 패널·어두운 스크림·그라데이션·블러·스포트라이트·글로우·종이 조각·테이프·브러시 칠·하이라이트 배경을 추가하지 않는다.
- 타이틀은 배경 이미지 위에 **글자만** 합성한다. 글자 그림자·네온·외곽선·입체 효과도 쓰지 않는다. 제목과 강조 span에는 `background:transparent; text-shadow:none; -webkit-text-stroke:0; box-shadow:none; filter:none; backdrop-filter:none`을 적용하고, 장식용 가상 요소를 만들지 않는다.
- 선택한 이미지 스타일의 피사체·재질 표현은 유지하되, 타이틀 영역을 강조하려고 별도의 효과를 생성하거나 덧씌우지 않는다. 배경 프롬프트에도 제목 뒤 박스·띠·스크림·광원 효과 금지를 명시한다.
- 가독성은 피사체 배치·자연스러운 여백·문구 길이·단색 글자색·허용된 제목 위치로 확보한다. 숏폼의 흰색 글자와 고정 좌표는 유지하며, 그 위치에서 읽히는 배경 구도를 설계한다. 읽기 어렵다는 이유로 효과를 다시 넣지 않는다. 추가 배경 생성이 필요하면 기존 비용 승인 범위를 따른다.
- 원본과 축소본에서 글자만 보이는지, 제목 뒤 국소적인 어두운 띠·밝은 판·블러가 없는지 실제 이미지로 확인한다. CSS의 효과 제거만으로 생성 이미지 안에 그려진 제목용 배경까지 통과시켜서는 안 된다.
- 이 규칙은 아래 가독성 처리와 기존 템플릿 지침보다 우선하며, 필수 스타일 질문 절차·선택한 서체·제목 규격은 유지한다.


## 지원 스타일

- 롱폼: 실사, 3D 그래픽, 두들·라인 일러스트, 페이퍼 콜라주.
- 숏폼·일반 가로형: 블루 시네마틱, SINSRA 오렌지 두들, 귀여운 카와이, 페이퍼 콜라주.
- 사선 양분할·중앙 양분할은 스타일과 별도로 검토하는 구도다.

## 롱폼 타이틀 헤드룸 (필수)

- 1280×720 롱폼 썸네일은 **캔버스 상단부터 최종 타이틀 글자 영역 위쪽까지 최소 72px(높이의 10%)**를 확보한다. 합성 템플릿의 기본 타이틀 상단은 96px로 둔다. 다른 출력 크기는 같은 비율로 환산한다.
- CSS `top` 값만 보고 통과시키지 않는다. 최종 서체를 로딩하고 문구 길이·폰트 크기·허용된 줄바꿈을 적용한 뒤 `Range.getBoundingClientRect()`로 실제 텍스트 영역의 상단을 측정한다. 최소값보다 위로 올라가면 타이틀 전체를 아래로 이동하고 다시 측정한다.
- 헤드룸은 상단의 시각적 숨 쉴 공간이다. 제목 위로 큰 피사체·장식·프레임·라벨을 밀어 넣지 않는다. 배경 이미지는 화면 끝까지 유지하며, 여백을 만들려고 흰 띠·검은 띠·판·그라데이션을 덧붙이지 않는다. 배경 프롬프트에서 상단의 자연스러운 여백을 확보한다.
- 가로 가운데 정렬과 한 줄 우선을 유지한다. 기본 제목은 위쪽 절반 안에 완전히 들어가야 한다. 헤드룸을 확보했더니 제목 하단이 360px를 넘으면 문구·줄바꿈·피사체 구도를 조정한다. 임의로 제목을 상단에 붙이거나 읽기 어려울 만큼 축소하지 않는다.
- 템플릿의 `headroomPx`, `minHeadroomPx`, `headroomPass`, `topHalfText`를 기록한다. `headroomPass:false`면 납품하지 않는다. 실제 원본·320×180 축소본에서 위 여백과 타이틀-피사체 관계도 확인한다. 숏폼의 고정 타이틀 좌표에는 이 롱폼 전용 수치를 적용하지 않는다.

